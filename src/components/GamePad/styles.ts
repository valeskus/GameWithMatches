import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

  gameButton: {
    width: 100,
  },

  gameContainerItem: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
  },

});
